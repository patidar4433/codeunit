extends layout

block content
  h1= title
  p Welcome to #{title} 
  p Welcome to #{title}
  p Password reset status :
  p email: #{email}
  p password: #{password}
  p confirm_password #{confirm_password}
  p message: #{message}
  p status #{status}